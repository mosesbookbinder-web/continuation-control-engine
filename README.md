# Continuation Control Engine

Continuation Control is a deterministic evaluation layer that determines
whether a declared process may continue under replay-verifiable evidence.

This repository provides the reference architecture for:

• the continuation kernel  
• artifact capture and witness chains  
• deterministic replay verification  
• research artifacts and papers  

Decision outcomes follow strict precedence:

HALT > INCOMPLETE > PASS
